version: '3.8'
services:
  whisper:
    build: .
    ports: ["9000:9000"]
    environment: [WHISPER_MODEL=small, PORT=9000]
    volumes: [whisper-cache:/root/.cache/huggingface]
    restart: unless-stopped
volumes:
  whisper-cache:
